# Week 3 Summary

## Focus

Forecast, Alerts, service layer, loading component, and data rendering practice.

## Work Covered

- Built Forecast page with prediction KPI cards.
- Added forecast graph placeholder and later improved it into chart work.
- Built Alerts page with alert cards and severity badges.
- Used `.map()` for rendering alert lists and forecast data.
- Created service files:
  - `dashboardService.js`
  - `forecastService.js`
  - `alertService.js`
- Refactored pages to load fake data from the service layer.
- Created reusable `Loading` component.

## Pending

- Real backend data integration is pending API availability.
- GIS map integration is pending GIS/API details.

## Outcome

Pages became cleaner because data logic moved into services, and list rendering became reusable across features.
